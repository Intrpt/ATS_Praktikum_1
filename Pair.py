class Pair:
    """docstring for Pair"""
    def __init__(self, vector_snapshot, vector_retina, ang_snapshot, ang_retina, size_snapsot, size_retina):
        super(Pair, self).__init__()
        self.vector_snapshot = vector_snapshot
        self.vector_retina = vector_retina
        self.vector_retina = vector_retina
        self.ang_snapshot = ang_snapshot
        self.ang_retina = ang_retina
        self.size_snapsot = size_snapsot
        self.size_retina = size_retina
        